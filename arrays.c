#include <stdio.h>

int main() {
    // Definindo um array de inteiros
    int arr[5] = {1, 2, 3, 4, 5};

    // Acessando elementos do array
    for (int i = 0; i < 5; i++) {
        printf("Element at index %d: %d\n", i, arr[i]);
    }

    return 0;
}
